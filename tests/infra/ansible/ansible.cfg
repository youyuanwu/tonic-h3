# Ansible defaults for the tonic-h3 benchmark connectivity tests.
# Run ansible-playbook from this directory (tests/infra/ansible/).
[defaults]
inventory = ./inventory.ini
host_key_checking = False
deprecation_warnings = False
retry_files_enabled = False

[ssh_connection]
pipelining = True
